<%= form_for(@exercise_event) do |f| %>
  <% if @exercise_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exercise_event.errors.count, "error") %> prohibited this exercise_event from being saved:</h2>

      <ul>
      <% @exercise_event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.label :date %>
    <input type="date" name="date" value=<%= Date.today %>></input>
  </div>
  <div class="field">
    <%= f.label :exercise_type_id %><br>
    <%= f.select :exercise_type_id, ExerciseType.all.collect { |p| [ p.name, p.id ] }, include_blank: true %>
  </div>
  <div class="field">
    <%= f.label :intensity_id %><br>
    <%= f.select :intensity_id, Intensity.all.collect { |p| [ p.name, p.id ] }, include_blank: true %>
  </div>
  <div class="field">
    <%= f.label :num_units %><br>
    <%= f.number_field :num_units %>
  </div>
  <div class="field">
    <%= f.label :exercise_unit_id %><br>
    <%= f.select :exercise_unit_id, ExerciseUnit.all.collect { |p| [ p.name, p.id ] }, include_blank: true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
